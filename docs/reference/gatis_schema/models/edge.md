---
sidebar_position: 1
---

# Edge

## Fields

| Name | Type | Description |
| -----: | :----: | ------------- |
| `edge_id` | [`Id`](../../overture/schema/system/ref/id.md) | A unique identifier for the edge. [NOTE: We will fill in instructions here on how to generate IDs, and we will also provide a data validator that may be capable of validating and helping to fill in these IDs.]<br/><br/>*`required at every tier`* |
| `geometry` | [`geometry`](../../system/geometric.md) | *Allowed geometry types: LineString* |
| `bbox` | [`bbox`](../../system/geometric.md) (optional) | An optional bounding box for the feature |
| `reference_ids[]` *(Road, Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | `list<`[`ReferenceId`](../reference_id.md)`>` (optional) | Can be used to add reference IDs to other datasources such as OSM, OpenLR, ARNOLD, HMPS, TIGER, Census road network, OSM, etc.). Should be an array of JSONs with the source name and ID pair. Each JSON should contain an ID field and source field at minimum. Can add other attributes such as the beginning and ending milepost from a linear referencing system.<br/><br/>*`optional at every tier`* |
| `reference_ids[].source` | `string` | Name of the dataset the id belongs to |
| `reference_ids[].id` | `string` | The identifier within that dataset |
| `street_name` *(Road)* | `string` | Specifies the name of a road or the road associated with the edge, such as the street along which a sidewalk or cycleway runs. In many cases, routing engines can fill in the closest street name for travelers to see. Use this field to specify the associated street explicitly or to correct an error within routing engines.<br/><br/>*`required at every tier`* |
| `edge_type` *(Road)* | `"road"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `from_node` *(Road, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | [`Id`](../../overture/schema/system/ref/id.md) (optional) | This field is used to identify the node where an edge begins. This information is needed for routing. Value needs to be from the nodes table in the node ID field.<br/><br/>*References `NodeBase` (association, starts at)*<br/>*`optional from tier 1; required from tier 2`* |
| `to_node` *(Road, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | [`Id`](../../overture/schema/system/ref/id.md) (optional) | This field is used to identify the node where an edge ends. This information is needed for routing. Value needs to be from the nodes table in the node ID field.<br/><br/>*References `NodeBase` (association, ends at)*<br/>*`optional from tier 1; required from tier 2`* |
| `directionality` *(Road, Escalator, Bikeway)* | [`Directionality`](types/directionality.md) | Specifies the directionality of the edge. If the edge is bidirectional, choose “both.” Used to help identify when bicycle infrastructure allows traffic in both directions. If left blank, then assumes 'both'.<br/><br/>*`required at every tier`* |
| `width` *(Road, Steps, Escalator)* | [`int32`](../../system/numeric.md) (optional) | Generalized width of the edge that best characterizes the width across its length. Measured in inches and rounded to the nearest inch. Cannot be negative. Note that it is assumed that 80' of height clearance is available for the full width given in this field.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`in`* |
| `width_min_passable` *(Road, Steps, Escalator)* | [`int32`](../../system/numeric.md) (optional) | The passable width of the edge at the point where it is narrowest. Measured in inches and rounded to the nearest inch. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`in`* |
| `width_tolerance` *(Road, Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | [`int32`](../../system/numeric.md) (optional) | Used to specify the tolerance of the width measurement in inches. Everything along the edge should be within +/- of this width.<br/><br/>*`optional at every tier`*<br/>*`in`* |
| `bridge` *(Road)* | `boolean` (optional) | Indicates if the edge is or is on a bridge. Can be used for any bridge type, including road bridges and pedestrian bridges. Reccomend marking roads with bike lanes that are bridges.<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `status` *(Road)* | [`Status`](types/status.md) (optional) | Most recent operating status of the segment. Whether the infrastructure is open and available for use. Default is 'open'<br/><br/>*`optional at every tier`* |
| `date_built` *(Road, Steps, Escalator, Ramp)* | `string` (optional) | Indicates when the facility was officially opened for use. If the facility has had a major remodeling where the structure, shape or another fundamental aspect was changed, the date of remodeling can be placed here. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional at every tier`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `check_date` *(Road, Steps, Escalator, Ramp)* | `string` (optional) | The date that this infrastructure was last inspected. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional at every tier`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `curb_height` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Indicates typical height of the curb along this segment of the road; can be used in deciding whether to route specific pedestrians off the sidewalk when there are obstructions or discontinuities. Measured and rounded to the nearest inch. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`in`* |
| `measured_length` *(Road, Steps, Escalator, Bikeway, Ramp, VirtualLink)* | [`float64`](../../system/numeric.md) (optional) | The measured length of the edge in feet. Note that geospatial data also contains a length attribute by default that may be useful in some cases. Measuring the traversable length of the segment is preferable.<br/><br/>*`optional at every tier`*<br/>*`ft`* |
| `traffic_volume` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Measures the motor vehicle annual average daily traffic (AADT) for the edge. Reported with no more than two significant figures. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`*<br/>*`AADT`* |
| `posted_speed_limit` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Used to indicate the posted speed limit. Measured in miles per hour. Cannot be negative. If used on bikeway, multi-use path, or trail, it's assumed that is the speed limit for non-motorized users.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`*<br/>*`mph`* |
| `mv_freeflow_speed` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Used to indicate the free-flow motor vehicle speed. Suggest using the 85th percentile. Measured in miles per hour. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`*<br/>*`mph`* |
| `thru_lanes` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Describes the number of vehicle thru lanes on an edge. Does not include turn lanes or shoulders. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`* |
| `aux_lanes` *(Road)* | [`int32`](../../system/numeric.md) (optional) | Describes the number of temporary vehicle lanes like turn lanes. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`* |
| `shoulder_width` *(Road)* | [`float64`](../../system/numeric.md) (optional) | Width of the paved shoulder that can be used by pedestrians or cyclists. Measured in feet with one decimal point of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 4`*<br/>*`ft`* |
| `roadway_centerline` *(Road)* | `boolean` (optional) | Indicates if there is a painted road centerline. Might be a useful attribute to track for finding low-stress streets.<br/><br/>*`optional from tier 1; recommended from tier 4`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `prohibited_uses` *(Road, Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | `list<`[`ProhibitedUses`](types/prohibited_uses.md)`>` (optional) | Specifies which types of users are legally prohibited from using the facility, based on the laws, policy, or signage on a facility (ex. “E-bikes prohibited on this trail”). Can provide one or multiple in list form.<br/><br/>*`optional at every tier`* |
| `allowed_uses` *(Road, Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp, VirtualLink)* | `list<`[`AllowedUses`](types/allowed_uses.md)`>` (optional) | Specifies exceptions to the usually prohibited users. Intended for designating whether bikes are allowed to use sidewalks, footpaths, and crossings for routing purposes.<br/><br/>*`optional at every tier`* |
| `traffic_calming` *(Road, Crossing)* | `list<string>` (optional) | Used to identify traffic calming features along a road, shared-use path or crossing. Implies that feature is present alongside the entirety of the edge. The fields that the traffic calming element(s) affect should be modified (i.e., a road narrowing should reduce the road's width field from its typical value). Recommended values are from https://www.ite.org/pub/?id=2a60c136-b1c0-b231-0522-ccbd075cac84 and https://wiki.openstreetmap.org/wiki/Key:traffic_calming Recommended values: for road edge type; corner extension / bulb out, choker, narrowed road, chicane, closure, mini roundabout, diagonal diverter, lateral shift, median barrier/forced turn island, raised intersection, realigned intersection, road narrowing, speed hump, speed table, traffic circle; for crossing edge type; raised crossing.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `seasonal[]` *(Road, Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp)* | `list<`[`SeasonalCondition`](../seasonal_condition.md)`>` (optional) | Indicates whether the segment is commonly affected by seasonal issues. Use this field for recurring (ex. yearly flooding) and not one-time (ex. single flood) events. Include both the seasonal concern and the season when it occurs as a JSON String. Recommended values: season; seasonal issues.<br/><br/>*`optional at every tier`* |
| `seasonal[].season` | `string` (optional) | spring, summer, fall or winter |
| `seasonal[].issue` | `string` (optional) | flooding, ice, snow, heavy rain, heat, low visibility, fog, wind |
| `surface_material` *(Road, Escalator)* | [`SurfaceMaterial`](types/surface_material.md) (optional) | Specifies the material used for the surface of the segment as of the inspection in 'check_date'<br/><br/>*`optional at every tier`* |
| `surface_issue` *(Road, Escalator, Bikeway)* | `string` (optional) | yes, no, cracking, scaling, spalling, uneven, frequent water pooling, heaving, missing bricks/stones, potholes/holes, slickness, detectable warning surface damage, longitudinal cracks and seams, other Recommended values: yes; no; cracking; scaling; spalling; uneven; frequent water pooling; heaving; missing bricks/stones; potholes/holes; slickness; detectable warning surface damage; longitudinal cracks and seams; metal plates; other.<br/><br/>*`optional at every tier`* |
| `incline` *(Road, Sidewalk, Crossing, Bikeway, MultiUsePath)* | [`float64`](../../system/numeric.md) (optional) | The running slope of the full segment. Assume the given incline is in the forward direction of the edge, regardless of edge directionality. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 2`*<br/>*`%`* |
| `cross_slope` *(Road, Bikeway)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at most points along its path. Cross slope is never reported in negative numbers. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`%`* |
| `cross_slope_max` *(Road, Bikeway, Trail)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at the point along its path where there is the greatest slope. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`%`* |
| `impediment` *(Road, Escalator, Bikeway)* | `list<string>` (optional) | Identifies the presence of an object that may pose a challenge for travelers passing along this edge. Mark an edge with this attribute only if the impediment is close enough to the footpath/pedestrian way or bike path to potentially pose a challenge. If left blank, the assumed value for this attribute is “unknown.” Recommended values: yes; no; low overgrowth (lower than 27'); high overgrowth (27' or higher); sign; low protrusion (lower than 27'); high protrusion (27' or higher); utility cover; stormwater grate; metal plate; metal decking (ex. on bridges); other surface impediment; other impediment.<br/><br/>*`optional at every tier`* |
| `tactile_marking` *(Road, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp)* | [`TactileMarking`](types/tactile_marking.md) (optional) | Indicates when tactile guidestrips or other markings are present to help identify the edge of a crosswalk or traffic island, the beginning or end of steps, or the presence of other infrastructure nearby, such as bike lanes. It is recommended to segment the edge so that this field is only equal to “yes” for the segment where the detectable warning appears. Do not use this field for tactile markings on curb ramps; instead, use the detectable_warning field for curb ramps.<br/><br/>*`optional at every tier`* |
| `road_associated` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Trail)* | `boolean` (optional) | Specifies if the edge is adjacent or associated to a road.<br/><br/>*`optional at every tier`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `street_name` *(Sidewalk, Crossing)* | `string` (optional) | Specifies the name of a road or the road associated with the edge, such as the street along which a sidewalk or cycleway runs. In many cases, routing engines can fill in the closest street name for travelers to see. Use this field to specify the associated street explicitly or to correct an error within routing engines.<br/><br/>*`optional from tier 1; required from tier 2`* |
| `facility_name` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator)* | `string` (optional) | The common name for this edge, by which travelers might recognize it.<br/><br/>*`optional at every tier`* |
| `edge_type` *(Sidewalk)* | `"sidewalk"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `from_node` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator)* | [`Id`](../../overture/schema/system/ref/id.md) (optional) | This field is used to identify the node where an edge begins. This information is needed for routing. Value needs to be from the nodes table in the node ID field.<br/><br/>*References `NodeBase` (association, starts at)*<br/>*`optional from tier 1; recommended from tier 3`* |
| `to_node` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator)* | [`Id`](../../overture/schema/system/ref/id.md) (optional) | This field is used to identify the node where an edge ends. This information is needed for routing. Value needs to be from the nodes table in the node ID field.<br/><br/>*References `NodeBase` (association, ends at)*<br/>*`optional from tier 1; recommended from tier 3`* |
| `directionality` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, MultiUsePath, Trail, Ramp, VirtualLink)* | [`Directionality`](types/directionality.md) (optional) | Specifies the directionality of the edge. If the edge is bidirectional, choose “both.” Used to help identify when bicycle infrastructure allows traffic in both directions. If left blank, then assumes 'both'.<br/><br/>*`optional at every tier`* |
| `width` *(Sidewalk, Crossing, Bikeway, MultiUsePath)* | [`int32`](../../system/numeric.md) (optional) | Generalized width of the edge that best characterizes the width across its length. Measured in inches and rounded to the nearest inch. Cannot be negative. Note that it is assumed that 80' of height clearance is available for the full width given in this field.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 2`*<br/>*`in`* |
| `width_min_passable` *(Sidewalk, Footpath, Crossing, TrafficIsland, MultiUsePath, Trail, Ramp)* | [`int32`](../../system/numeric.md) (optional) | The passable width of the edge at the point where it is narrowest. Measured in inches and rounded to the nearest inch. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`in`* |
| `bridge` *(Sidewalk, Footpath)* | `boolean` (optional) | Indicates if the edge is or is on a bridge. Can be used for any bridge type, including road bridges and pedestrian bridges. Reccomend marking roads with bike lanes that are bridges.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `status` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail, Ramp)* | [`Status`](types/status.md) (optional) | Most recent operating status of the segment. Whether the infrastructure is open and available for use. Default is 'open'<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`* |
| `date_built` *(Sidewalk, Footpath, Crossing, TrafficIsland, Bikeway, MultiUsePath, Trail)* | `string` (optional) | Indicates when the facility was officially opened for use. If the facility has had a major remodeling where the structure, shape or another fundamental aspect was changed, the date of remodeling can be placed here. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `check_date` *(Sidewalk, Footpath, Crossing, TrafficIsland, Bikeway, MultiUsePath, Trail)* | `string` (optional) | The date that this infrastructure was last inspected. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `presence` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail)* | [`FeaturePresence`](types/feature_presence.md) (optional) | Indicates whether the piece of infrastructure exists or is present. When other attributes are provided, the existence of the infrastructure can be assumed. This attribute is useful for identifying where a sidewalk or a crossing might be missing or where its presence is unknown. Conditionally required if no other identfiying fields supplied.<br/><br/>*`optional at every tier`* |
| `measured_length` *(Sidewalk, Crossing)* | [`float64`](../../system/numeric.md) (optional) | The measured length of the edge in feet. Note that geospatial data also contains a length attribute by default that may be useful in some cases. Measuring the traversable length of the segment is preferable.<br/><br/>*`optional from tier 1; recommended from tier 2`*<br/>*`ft`* |
| `separation_elements` *(Sidewalk, Footpath)* | `list<string>` (optional) | The materials used to separate the cycleway or footpath from motor vehicle traffic -- for example, as part of a buffer. Recommended values: bollards; concrete barrier; parking; median; trees.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `separation_permeable_car` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | [`SeparationPermeableCar`](types/separation_permeable_car.md) (optional) | Can a vehicle easily access this edge? Primarily intended for bikeways but could be used for pedestrian facilities.<br/><br/>*`optional at every tier`* |
| `buffer_width` *(Sidewalk, Footpath, Bikeway)* | [`float64`](../../system/numeric.md) (optional) | Distance between the edge of the motor vehicle travel lane and the bike lane or sidewalk. Measured in feet and rounded to the nearest half foot. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`ft`* |
| `street_parking` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | [`StreetParking`](types/street_parking.md) (optional) | Field intended to indicate orientation of street parking in relation to a bike facility. Floating street parking is also referred to as parking protected.<br/><br/>*`optional at every tier`* |
| `street_parking_buffer` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | [`float64`](../../system/numeric.md) (optional) | The space between a bicycle facility and the street parking. Measured in feet and rounded to the nearest half foot. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`ft`* |
| `surface_material` *(Sidewalk, Crossing, Bikeway, MultiUsePath, Trail, Ramp)* | [`SurfaceMaterial`](types/surface_material.md) (optional) | Specifies the material used for the surface of the segment as of the inspection in 'check_date'<br/><br/>*`optional from tier 1; required from tier 2`* |
| `surface_issue` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, MultiUsePath, Trail, Ramp)* | `string` (optional) | yes, no, cracking, scaling, spalling, uneven, frequent water pooling, heaving, missing bricks/stones, potholes/holes, slickness, detectable warning surface damage, longitudinal cracks and seams, other Recommended values: yes; no; cracking; scaling; spalling; uneven; frequent water pooling; heaving; missing bricks/stones; potholes/holes; slickness; detectable warning surface damage; longitudinal cracks and seams; metal plates; other.<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `pedestrian_lane` *(Sidewalk)* | `string` (optional) | Indicates whether the stretch of sidewalk is a pedestrian lane, in which a section of the roadway surface is divided out for pedestrian use.<br/><br/>*`optional from tier 1; recommended from tier 2`* |
| `cross_slope` *(Sidewalk, Crossing)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at most points along its path. Cross slope is never reported in negative numbers. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 2`*<br/>*`%`* |
| `cross_slope_max` *(Sidewalk, Footpath, Crossing, TrafficIsland, MultiUsePath, Ramp)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at the point along its path where there is the greatest slope. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`%`* |
| `ada_compliance_date` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail, Ramp)* | `string` (optional) | Indicates the date when ADA compliance was assessed. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.. This field is conditionally required if 'ada_compliant_with' is filled out.<br/><br/>*`conditionally_required at every tier`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliant_with` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail, Ramp)* | [`AdaCompliantWith`](types/ada_compliant_with.md) (optional) | If this infrastructure has been assessed for ADA compliance, the specific ADA guidelines or standards used in the assessment. Also fill out 'ada_compliance_date.' If no ADA assessment is being reported, leave blank.<br/><br/>*`conditionally_required at every tier`* |
| `impediment` *(Sidewalk, Footpath, Crossing, TrafficIsland, Steps, MultiUsePath, Trail, Ramp)* | `list<string>` (optional) | Identifies the presence of an object that may pose a challenge for travelers passing along this edge. Mark an edge with this attribute only if the impediment is close enough to the footpath/pedestrian way or bike path to potentially pose a challenge. If left blank, the assumed value for this attribute is “unknown.” Recommended values: yes; no; low overgrowth (lower than 27'); high overgrowth (27' or higher); sign; low protrusion (lower than 27'); high protrusion (27' or higher); utility cover; stormwater grate; metal plate; metal decking (ex. on bridges); other surface impediment; other impediment.<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `visual_markings` *(Sidewalk, Footpath, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | `string` (optional) | The way the crossing is marked within the roadway space. “Standard” means two solid parallel lines that indicate the outline, “dashed lines” means two dashed parallel lines that indicate the outline, “zebra” means regularly spaced diagonal bars along its length, “continental” means regularly spaced horizontal bars along its length, and “ladder” means standard plus either zebra or continental. Recommended values: yes; no; dashed lines; zebra; continental; ladder; transverse; other.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `tactile_marking` *(Sidewalk, Footpath, Crossing, TrafficIsland)* | [`TactileMarking`](types/tactile_marking.md) (optional) | Indicates when tactile guidestrips or other markings are present to help identify the edge of a crosswalk or traffic island, the beginning or end of steps, or the presence of other infrastructure nearby, such as bike lanes. It is recommended to segment the edge so that this field is only equal to “yes” for the segment where the detectable warning appears. Do not use this field for tactile markings on curb ramps; instead, use the detectable_warning field for curb ramps.<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`* |
| `street_name` *(Footpath, TrafficIsland, Steps, Escalator)* | `string` (optional) | Specifies the name of a road or the road associated with the edge, such as the street along which a sidewalk or cycleway runs. In many cases, routing engines can fill in the closest street name for travelers to see. Use this field to specify the associated street explicitly or to correct an error within routing engines.<br/><br/>*`optional at every tier`* |
| `edge_type` *(Footpath)* | `"footpath"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `width` *(Footpath, TrafficIsland, Trail, Ramp)* | [`int32`](../../system/numeric.md) (optional) | Generalized width of the edge that best characterizes the width across its length. Measured in inches and rounded to the nearest inch. Cannot be negative. Note that it is assumed that 80' of height clearance is available for the full width given in this field.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 3`*<br/>*`in`* |
| `measured_length` *(Footpath, TrafficIsland, MultiUsePath, Trail)* | [`float64`](../../system/numeric.md) (optional) | The measured length of the edge in feet. Note that geospatial data also contains a length attribute by default that may be useful in some cases. Measuring the traversable length of the segment is preferable.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*`ft`* |
| `surface_material` *(Footpath, TrafficIsland, Steps)* | [`SurfaceMaterial`](types/surface_material.md) (optional) | Specifies the material used for the surface of the segment as of the inspection in 'check_date'<br/><br/>*`optional from tier 1; required from tier 3`* |
| `incline` *(Footpath, TrafficIsland, Ramp)* | [`float64`](../../system/numeric.md) (optional) | The running slope of the full segment. Assume the given incline is in the forward direction of the edge, regardless of edge directionality. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 3`*<br/>*`%`* |
| `cross_slope` *(Footpath)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at most points along its path. Cross slope is never reported in negative numbers. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 3`*<br/>*`%`* |
| `edge_type` *(Crossing)* | `"crossing"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `bridge` *(Crossing, TrafficIsland, Steps, Escalator, Bikeway, MultiUsePath, Trail)* | `boolean` (optional) | Indicates if the edge is or is on a bridge. Can be used for any bridge type, including road bridges and pedestrian bridges. Reccomend marking roads with bike lanes that are bridges.<br/><br/>*`optional at every tier`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `separation_elements` *(Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | `list<string>` (optional) | The materials used to separate the cycleway or footpath from motor vehicle traffic -- for example, as part of a buffer. Recommended values: bollards; concrete barrier; parking; median; trees.<br/><br/>*`optional at every tier`* |
| `buffer_width` *(Crossing, TrafficIsland, Steps, Escalator, MultiUsePath, Trail)* | [`float64`](../../system/numeric.md) (optional) | Distance between the edge of the motor vehicle travel lane and the bike lane or sidewalk. Measured in feet and rounded to the nearest half foot. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`ft`* |
| `vehicle_traffic_control` *(Crossing)* | [`VehicleTrafficControl`](types/vehicle_traffic_control.md) (optional) | Describes how motor vehicle traffic that passes through the crossing space is controlled.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `cross_vehicle_traffic_control` *(Crossing)* | `list<`[`CrossVehicleTrafficControl`](types/cross_vehicle_traffic_control.md)`>` (optional) | Describes how motor vehicle traffic coming from cross streets is controlled. This traffic may or may not turn into the crossing space. For intersections with more than one cross street, list all of the control types present.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `ped_traffic_control` *(Crossing)* | [`PedTrafficControl`](types/ped_traffic_control.md) (optional) | Describes the type of signal that controls the timing of pedestrian use of the crossing.<br/><br/>*`optional from tier 1; required from tier 3`* |
| `ped_protection` *(Crossing)* | `list<`[`PedProtection`](types/ped_protection.md)`>` (optional) | Lists any features of traffic control that are intended to improve the protection of pedestrians while crossing.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `rail_crossing` *(Crossing)* | `boolean` (optional) | Used to indicate if a pedestrian or bicycle crossing is a railroad crossing. Use generic nodes to connect a rail crossing to the rest of the network. Note that there is also a value of 'rail tracks' for the other_issue attribute for edges. Use rail_crossing for track crossings that people walking, rolling or biking will need to cross, and that have active rail traffic. The 'rail tracks' value for other_issue can be used on other edge types or to identify remaining or unused tracks no longer traveled by trains.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `visual_markings` *(Crossing)* | `string` (optional) | The way the crossing is marked within the roadway space. “Standard” means two solid parallel lines that indicate the outline, “dashed lines” means two dashed parallel lines that indicate the outline, “zebra” means regularly spaced diagonal bars along its length, “continental” means regularly spaced horizontal bars along its length, and “ladder” means standard plus either zebra or continental. Recommended values: yes; no; dashed lines; zebra; continental; ladder; transverse; other.<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`* |
| `edge_type` *(TrafficIsland)* | `"traffic_island"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `cross_slope` *(TrafficIsland, MultiUsePath, Trail, Ramp)* | [`float64`](../../system/numeric.md) (optional) | The cross slope of the edge at most points along its path. Cross slope is never reported in negative numbers. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`%`* |
| `edge_type` *(Steps)* | `"steps"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `wheel_channel` *(Steps)* | `boolean` (optional) | Whether there is a wheel channel to allow for pushing a bicycle up the stairs.<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `step_count` *(Steps)* | [`int32`](../../system/numeric.md) (optional) | The number of steps that make up this set of stairs. Must be greater than zero.<br/><br/>*`> 0`*<br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `handrail` *(Steps, Ramp)* | `boolean` (optional) | Whether a handrail is available on this set of stairs.<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `edge_type` *(Escalator)* | `"escalator"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `road_associated` *(Bikeway, MultiUsePath, VirtualLink)* | `boolean` | Specifies if the edge is adjacent or associated to a road.<br/><br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`*<br/>*`required at every tier`* |
| `street_name` *(Bikeway, MultiUsePath, Trail)* | `string` (optional) | Specifies the name of a road or the road associated with the edge, such as the street along which a sidewalk or cycleway runs. In many cases, routing engines can fill in the closest street name for travelers to see. Use this field to specify the associated street explicitly or to correct an error within routing engines.<br/><br/>*`optional from tier 1; recommended from tier 2`* |
| `facility_name` *(Bikeway, MultiUsePath, Trail)* | `string` (optional) | The common name for this edge, by which travelers might recognize it.<br/><br/>*`optional from tier 1; recommended from tier 2`* |
| `edge_type` *(Bikeway)* | `"bikeway"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `width_min_passable` *(Bikeway)* | [`int32`](../../system/numeric.md) (optional) | The passable width of the edge at the point where it is narrowest. Measured in inches and rounded to the nearest inch. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 3`*<br/>*`in`* |
| `bikeway_type` *(Bikeway)* | `string` | Common name used for the bicycle facility type. Should align with the National Bikeway Network, NACTO, or AASHTO facility types. Recommended values: Bike Lane; Buffered Bike Lane; Separated Bike Lane; Counter-Flow Bike Lane; Paved Shoulder; Shared Lane.<br/><br/>*`required at every tier`* |
| `bikeway_grade_separation` *(Bikeway)* | [`BikewayGradeSeparation`](types/bikeway_grade_separation.md) (optional) | The vertical level of the bikeway with respect to the road. Not meant for bikeways that are not road associated.<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`* |
| `separation_elements` *(Bikeway)* | `list<string>` (optional) | The materials used to separate the cycleway or footpath from motor vehicle traffic -- for example, as part of a buffer. Recommended values: bollards; concrete barrier; parking; median; trees.<br/><br/>*`optional from tier 1; recommended from tier 1; required from tier 2`* |
| `separation_permeable_car` *(Bikeway)* | [`SeparationPermeableCar`](types/separation_permeable_car.md) (optional) | Can a vehicle easily access this edge? Primarily intended for bikeways but could be used for pedestrian facilities.<br/><br/>*`optional from tier 1; recommended from tier 1; required from tier 2`* |
| `street_parking` *(Bikeway)* | [`StreetParking`](types/street_parking.md) (optional) | Field intended to indicate orientation of street parking in relation to a bike facility. Floating street parking is also referred to as parking protected.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `street_parking_buffer` *(Bikeway)* | [`float64`](../../system/numeric.md) (optional) | The space between a bicycle facility and the street parking. Measured in feet and rounded to the nearest half foot. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 2`*<br/>*`ft`* |
| `posted_speed_limit` *(Bikeway, MultiUsePath, Trail)* | [`int32`](../../system/numeric.md) (optional) | Used to indicate the posted speed limit. Measured in miles per hour. Cannot be negative. If used on bikeway, multi-use path, or trail, it's assumed that is the speed limit for non-motorized users.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`mph`* |
| `edge_type` *(MultiUsePath)* | `"multi_use_path"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `mup_modal_delineation` *(MultiUsePath)* | `boolean` (optional) | Designates whether bikes and pedestrians have designated spaces on a multi-use/shared-use path, or whether all travelers use the same space.<br/><br/>*`optional from tier 1; recommended from tier 2`*<br/>*`BeforeValidator(func=<function _parse_yes_no at 0x103912c00>, json_schema_input_type=PydanticUndefined)`*<br/>*`PlainSerializer(func=<function <lambda> at 0x103912ca0>, return_type=<class 'str'>, when_used='always')`* |
| `edge_type` *(Trail)* | `"trail"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `official` *(Trail)* | `list<string>` (optional) | Indicates whether a trail has been officially designated by a government body or other recognized organization, with a string of the name of the recognizing body and/or a URL to the source/reference to the recognition for users to validate/verify/see additional information.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `incline` *(Trail)* | [`float64`](../../system/numeric.md) (optional) | The running slope of the full segment. Assume the given incline is in the forward direction of the edge, regardless of edge directionality. Report as percentage of the slope, with two decimal points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional at every tier`*<br/>*`%`* |
| `edge_type` *(Ramp)* | `"ramp"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
| `edge_type` *(VirtualLink)* | `"virtual_link"` | Identifies the edge type. Also used for assigning attributes that need to be filled in.<br/><br/>*`required at every tier`* |
