---
sidebar_position: 1
---

# Node

## Fields

| Name | Type | Description |
| -----: | :----: | ------------- |
| `node_id` | [`Id`](../../overture/schema/system/ref/id.md) | A unique identifier for the node. [NOTE: We will fill in instructions here on how to generate IDs, and we will also provide a data validator that may be capable of validating and helping to fill in these IDs.]<br/><br/>*`required at every tier`* |
| `geometry` | [`geometry`](../../system/geometric.md) | *Allowed geometry types: Point* |
| `bbox` | [`bbox`](../../system/geometric.md) (optional) | An optional bounding box for the feature |
| `node_type` *(Generic)* | `"generic"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `curb_ramp_system_id` *(Generic)* | `string` (optional) | An identifier to link any nodes and/or edges that are involved in the same 'curb ramp system', which we define as the network of elements that sidewalk users use to transition from a sidewalk to a crossing or other pedestrian edge. For clarity, use the same curb_ramp_system_id for all elements of the same ramp system. The ID can be any unique number to the system. Curb ramp systems may include sidewalk edges, curb_ramp_toplanding, curb_ramp_runslope, and crosswalk edges; as well as sidewalk_to_ramp_transition, ramp_to_street_transition or generic nodes.<br/><br/>*`optional at every tier`* |
| `reference_ids[]` *(Generic, CurbRamp, SidewalkToRampTransition, RampToStreetTransition)* | `list<`[`ReferenceId`](../reference_id.md)`>` (optional) | Can be used to add reference IDs to other datasources such as OSM, OpenLR, ARNOLD, HMPS, TIGER, Census road network, OSM, etc.). Should be an array of JSONs with the source name and ID pair. Each JSON should contain an ID field and source field at minimum.<br/><br/>*`optional at every tier`* |
| `reference_ids[].source` | `string` (optional) | Name of the dataset the id belongs to |
| `reference_ids[].id` | `string` (optional) | The identifier within that dataset |
| `curb_type` *(Generic)* | [`CurbType`](types/curb_type.md) (optional) | The type of curb that is present, if there is not a curb ramp.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `rail_crossing_control` *(Generic)* | `list<`[`RailCrossingControl`](types/rail_crossing_control.md)`>` (optional) | Describes the pedestrian traffic warnings and controls in the approach to the rail crossing. The nodes mark the location of the controls on each side of the tracks. Mark the crossing itself as a crossing edge with attribute rail_crossing=yes.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `node_type` *(CurbRamp)* | `"curb_ramp"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `presence` *(CurbRamp)* | [`FeaturePresence`](types/feature_presence.md) (optional) | Indicates whether the piece of infrastructure exists or is present. When other attributes are provided, the existence of the infrastructure can be assumed. This attribute is useful for identifying where a curb ramp or other infrastructure might be missing or where its presence is unknown. Conditionally required if no other identifying attributes provided.<br/><br/>*`optional at every tier`* |
| `date_built` *(CurbRamp, SidewalkToRampTransition, RampToStreetTransition)* | `string` (optional) | When the facility was officially opened for use. date_built represents the original opening date. Use the Events extension to record details about construction history, remodeling, removal and other physical changes. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `last_inspection_date` *(CurbRamp, SidewalkToRampTransition, RampToStreetTransition)* | `string` (optional) | The date that this infrastructure was last inspected. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliance_date` *(CurbRamp)* | `string` (optional) | Indicates the date when ADA compliance was assessed. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available. This field is conditionally required if 'ada_compliant_with' is filled out.<br/><br/>*`optional from tier 1; recommended from tier 2`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliant_with` *(CurbRamp)* | [`NodeAdaCompliantWith`](types/node_ada_compliant_with.md) (optional) | If this infrastructure has been assessed for ADA compliance, the specific ADA guidelines or standards used in the assessment. Also fill out 'ada_compliance_date.' If no ADA assessment is being reported, leave blank.<br/><br/>*`optional from tier 1; recommended from tier 2`* |
| `incline` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the incline or running slope of the ramp, following the possible directions of travel. Reported as a decimal number describing the percentage of the slope, with two points of precision.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*`%`* |
| `cross_slope` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the cross slope of the ramp, which runs perpendicular to the incline / directions of travel. Reported as a decimal number describing the percentage of the slope, with two points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; recommended from tier 3`*<br/>*`%`* |
| `width_in` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the minimum width of the ramp, measured between the handrails if handrails are provided. Reported as a whole number rounded to the nearest inch. Note that it is assumed that 80' of height clearance is available for the full width given in this field.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*`in`* |
| `ramp_type` *(CurbRamp)* | `string` (optional) | Indicates the orientation of the ramp in relation to the pedestrian direction of travel at the location. Where a double curb ramp exists, map each ramp as a separate curb_ramp node. Recommended values: diagonal; directional; parallel; perpendicular; built-up; combination; transition; cut-through (median/island ramp); unknown.<br/><br/>*`optional at every tier`*<br/>*A vocabulary v1.0 publishes for a field whose type it leaves open. (`SuggestedValues`)* |
| `detectable_warning` *(CurbRamp)* | [`DetectableWarning`](types/detectable_warning.md) (optional) | Describes whether tactile paving is present, and whether or not it has a constrasting color (which should meet ADA guidelines for the amount of contrast).<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `impediment` *(CurbRamp)* | `list<`[`Impediment`](types/impediment.md)`>` (optional) | Identifies the presence of an object that may pose a challenge for travelers passing through this node. Mark a node with this attribute only if the impediment is close enough to the pedestrian way or bike path to potentially pose a challenge. If left blank, the assumed value for this attribute is “unknown.”<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `surface_issue` *(CurbRamp)* | `list<`[`NodeSurfaceIssue`](types/node_surface_issue.md)`>` (optional) | Description of surface quality issues that may pose a challenge for travelers passing through this node.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `status` *(CurbRamp)* | [`NodeStatus`](types/node_status.md) (optional) | Most recent operating status of the node. Whether the infrastructure is open and available for use. If left blank, status is assumed 'open'<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `other_issue` *(CurbRamp)* | [`OtherIssue`](types/other_issue.md) (optional) | Identifies whether this node has another type of issue that may pose a challenge for travelers, besides impediments and surface damage. Includes design, construction and other issue types.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `last_inspection_type` *(CurbRamp)* | `string` (optional) | The type of inspection that was carried out on the piece of infrastructure, on the date listed under last_inspection_date. Recommended values: routine maintenance check; ADA; safety audit; construction inspection; post-crash audit; other.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*A vocabulary v1.0 publishes for a field whose type it leaves open. (`SuggestedValues`)* |
| `lifecycle_stage` *(CurbRamp)* | `string` (optional) | The lifecycle stage of this piece of infrastructure, as of the last_inspection_date. Recommended values: new; operational; nearing replacement; replacement planned or in planning.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*A vocabulary v1.0 publishes for a field whose type it leaves open. (`SuggestedValues`)* |
| `maintenance_schedule` *(CurbRamp)* | `string` (optional) | Description of the maintenance schedule, frequency of inspection, replacement schedule or other information about when the piece of infrastructure is maintained.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `planned_work` *(CurbRamp)* | `string` (optional) | Description of any planned work ahead for the infrastructure. This may include plans for construction or remodeling, upcoming work orders or other types of planned improvements.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `owner` *(CurbRamp)* | `string` (optional) | The entity that owns this piece of infrastructure. If a department, office or subagency is responsible for the infrastructure, list that department, office or subagency.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `maintainer` *(CurbRamp)* | `string` (optional) | The entity that is responsible for maintaining this piece of infrastructure. It may or may not be the same as owner. If a department, office or subagency is responsible for the infrastructure, list that department, office or subagency.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `node_type` *(SidewalkToRampTransition)* | `"sidewalk_to_ramp_transition"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `curb_ramp_system_id` *(SidewalkToRampTransition, RampToStreetTransition)* | `string` | An identifier to link any nodes and/or edges that are involved in the same 'curb ramp system', which we define as the network of elements that sidewalk users use to transition from a sidewalk to a crossing or other pedestrian edge. For clarity, use the same curb_ramp_system_id for all elements of the same ramp system. The ID can be any unique number to the system. Curb ramp systems may include sidewalk edges, curb_ramp_toplanding, curb_ramp_runslope, and crosswalk edges; as well as sidewalk_to_ramp_transition, ramp_to_street_transition or generic nodes.<br/><br/>*`required at every tier`* |
| `ada_compliance_date` *(SidewalkToRampTransition, RampToStreetTransition)* | `string` (optional) | Indicates the date when ADA compliance was assessed. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available. This field is conditionally required if 'ada_compliant_with' is filled out.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliant_with` *(SidewalkToRampTransition, RampToStreetTransition)* | [`NodeAdaCompliantWith`](types/node_ada_compliant_with.md) (optional) | If this infrastructure has been assessed for ADA compliance, the specific ADA guidelines or standards used in the assessment. Also fill out 'ada_compliance_date.' If no ADA assessment is being reported, leave blank.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `node_type` *(RampToStreetTransition)* | `"ramp_to_street_transition"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
