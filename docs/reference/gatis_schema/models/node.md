---
sidebar_position: 1
---

# Node

## Fields

| Name | Type | Description |
| -----: | :----: | ------------- |
| `node_id` | [`Id`](../../overture/schema/system/ref/id.md) | A unique identifier for the node. [NOTE: We will fill in instructions here on how to generate IDs, and we will also provide a data validator that may be capable of validating and helping to fill in these IDs.]<br/><br/>*`required at every tier`* |
| `geometry` | [`geometry`](../../system/geometric.md) | *Allowed geometry types: Point* |
| `bbox` | [`bbox`](../../system/geometric.md) (optional) | An optional bounding box for the feature |
| `node_type` *(Virtual)* | `"virtual"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `reference_ids[]` *(Virtual, CurbRamp, Elevator, TransitStop, Issue, TrafficCalming)* | `list<`[`ReferenceId`](../reference_id.md)`>` (optional) | Can be used to add reference IDs to other datasources such as OSM, OpenLR, ARNOLD, HMPS, TIGER, Census road network, OSM, etc.). Should be an array of JSONs with the source name and ID pair. Each JSON should contain an ID field and source field at minimum. Can add other attributes such as the beginning and ending milepost from a linear referencing system.<br/><br/>*`optional at every tier`* |
| `reference_ids[].source` | `string` | Name of the dataset the id belongs to |
| `reference_ids[].id` | `string` | The identifier within that dataset |
| `curb_type` *(Virtual)* | [`CurbType`](types/curb_type.md) (optional) | The type of curb that is present, if there is not a curb ramp.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `rail_crossing` *(Virtual)* | `list<`[`RailCrossing`](types/rail_crossing.md)`>` (optional) | Describes the pedestrian traffic warnings and controls in the approach to the rail crossing. The nodes mark the location of the controls on each side of the tracks. Mark the crossing itself as a virtual link (edge).<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `node_type` *(CurbRamp)* | `"curb_ramp"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `presence` *(CurbRamp, Elevator, TrafficCalming)* | [`FeaturePresence`](types/feature_presence.md) (optional) | Indicates whether the piece of infrastructure exists or is present. When other attributes are provided, the existence of the infrastructure can be assumed. This attribute is useful for identifying where a curb ramp or other infrastructure might be missing or where its presence is unknown. Conditionally required if no other identifying attributes provided.<br/><br/>*`optional at every tier`* |
| `date_built` *(CurbRamp)* | `string` (optional) | When the facility was officially opened for use. If the facility has had a major remodeling where the structure, shape or another fundamental aspect was changed, the date of remodeling can be placed here. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 2`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `check_date` *(CurbRamp)* | `string` (optional) | The date that this infrastructure was last inspected. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 2`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliance_date` *(CurbRamp, Elevator)* | `string` (optional) | Indicates the date when ADA compliance was assessed. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available. This field is conditionally required if 'ada_compliant_with' is filled out.<br/><br/>*`conditionally_required at every tier`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `ada_compliant_with` *(CurbRamp, Elevator)* | [`AdaCompliantWith`](types/ada_compliant_with.md) (optional) | If this infrastructure has been assessed for ADA compliance, the specific ADA guidelines or standards used in the assessment. Also fill out 'ada_compliance_date.' If no ADA assessment is being reported, leave blank.<br/><br/>*`conditionally_required at every tier`* |
| `incline` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the incline or running slope of the ramp, following the possible directions of travel. Reported as a decimal number describing the percentage of the slope, with two points of precision.<br/><br/>*`optional from tier 1; required from tier 3`*<br/>*`%`* |
| `cross_slope` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the cross slope of the ramp, which runs perpendicular to the incline / directions of travel. Reported as a decimal number describing the percentage of the slope, with two points of precision. Cannot be negative.<br/><br/>*`≥ 0`*<br/>*`optional from tier 1; required from tier 3`*<br/>*`%`* |
| `width` *(CurbRamp)* | [`float64`](../../system/numeric.md) (optional) | Indicates the minimum width of the ramp, measured between the handrails if handrails are provided. Reported as a whole number rounded to the nearest inch. Note that it is assumed that 80' of height clearance is available for the full width given in this field.<br/><br/>*`optional from tier 1; required from tier 3`*<br/>*`in`* |
| `ramp_type` *(CurbRamp)* | `string` (optional) | Indicates the orientation of the ramp in relation to the pedestrian direction of travel at the location. Where a double curb ramp exists, map each ramp as a separate curb_ramp node. Recommended values: diagonal; parallel; perpendicular; unknown.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `detectable_warning` *(CurbRamp)* | [`DetectableWarning`](types/detectable_warning.md) (optional) | Describes whether tactile paving is present, and whether or not it has a constrasting color (which should meet ADA guidelines for the amount of contrast).<br/><br/>*`optional from tier 1; required from tier 3`* |
| `impediment` *(CurbRamp, Issue)* | `list<`[`Impediment`](types/impediment.md)`>` (optional) | Identifies the presence of an object that may pose a challenge for travelers passing through this node. Mark a node with this attribute only if the impediment is close enough to the footpath/pedestrian way or bike path to potentially pose a challenge. If left blank, the assumed value for this attribute is “unknown.”<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `surface_issue` *(CurbRamp, Issue)* | `list<`[`SurfaceIssue`](types/surface_issue.md)`>` (optional) | Description of surface quality issues that may pose a challenge for travelers passing through this node.<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `status` *(CurbRamp)* | [`Status`](types/status.md) (optional) | Most recent operating status of the node. Whether the infrastructure is open and available for use. Default is 'open'<br/><br/>*`optional from tier 1; recommended from tier 2; required from tier 3`* |
| `node_type` *(Elevator)* | `"elevator"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `date_built` *(Elevator, TrafficCalming)* | `string` (optional) | When the facility was officially opened for use. If the facility has had a major remodeling where the structure, shape or another fundamental aspect was changed, the date of remodeling can be placed here. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `check_date` *(Elevator, Issue, TrafficCalming)* | `string` (optional) | The date that this infrastructure was last inspected. Report in RFC 3339 format containing day, month and year, or just month and year or year if day or month is not available.<br/><br/>*`optional from tier 1; recommended from tier 3`*<br/>*Generic pattern-based string constraint. (`PatternConstraint`, pattern: `^\d{4}(-\d{2}(-\d{2})?)?$`)* |
| `status` *(Elevator)* | [`Status`](types/status.md) (optional) | Most recent operating status of the node. Whether the infrastructure is open and available for use. Default is 'open'<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `node_type` *(TransitStop)* | `"transit_stop"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `gtfs_id[]` *(TransitStop)* | `list<`[`GtfsReference`](../gtfs_reference.md)`>` (optional) | An array of objects containing keys for GTFS agency_id and stop_id from an existing GTFS dataset for this transit stop. Represent as string with the format '{agency_id},{stop_id}'. These IDs can be crosswalked with GTFS and TIDES data to obtain additional stop attributes and connect to the broader transit network. See GTFS documentation for more information.<br/><br/>*`optional from tier 1; recommended from tier 3`* |
| `gtfs_id[].agency_id` | `string` | GTFS agency_id |
| `gtfs_id[].stop_id` | `string` | GTFS stop_id |
| `node_type` *(Issue)* | `"issue"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `node_type` *(TrafficCalming)* | `"traffic_calming"` | Indicates the type of node.<br/><br/>*`required at every tier`* |
| `traffic_calming_type` *(TrafficCalming)* | `string` (optional) | Used to identify traffic calming features on a road or crossing. Used when it makes more sense to model the traffic calming feature as a node instead of an edge. Recommended values are from https://www.ite.org/pub/?id=2a60c136-b1c0-b231-0522-ccbd075cac84 and https://wiki.openstreetmap.org/wiki/Key:traffic_calming<br/><br/>*`optional from tier 1; recommended from tier 3; required from tier 4`* |
| `status` *(TrafficCalming)* | [`Status`](types/status.md) (optional) | Most recent operating status of the node. Whether the infrastructure is open and available for use. Default is 'open'<br/><br/>*`optional at every tier`* |
